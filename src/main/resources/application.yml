spring:
  application:
    name: prenota24
  datasource:
    url: ${DB_URL:jdbc:postgresql://aws-0-eu-west-1.pooler.supabase.com:5432/postgres}
    username: ${DB_USERNAME:postgres.wrvqowbpalfzxlmutiqq}
    password: ${DB_PASSWORD:Prenota24!!!}

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  flyway:
    enabled: true

  mail:
    host: ${MAIL_HOST:smtp.gmail.com}
    port: ${MAIL_PORT:587}
    username: ${MAIL_USERNAME:}
    password: ${MAIL_PASSWORD:}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
        from: ${MAIL_FROM:noreply@prenota24.it}

  task:
    execution:
      pool:
        core-size: 4
        max-size: 16

jwt:
  secret: ${JWT_SECRET:dev-only-secret-change-in-production-min-32-chars!!}
  expiration-hours: ${JWT_EXPIRATION_HOURS:24}
