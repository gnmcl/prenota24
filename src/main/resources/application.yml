spring:
  application:
    name: prenota24
  datasource:
    url: ${DB_URL:jdbc:postgresql://aws-0-eu-west-1.pooler.supabase.com:6543/postgres?pgbouncer=true&prepareThreshold=0}
    username: ${DB_USERNAME:postgres.wrvqowbpalfzxlmutiqq}
    password: ${DB_PASSWORD:Prenota24!!!}
    hikari:
      maximum-pool-size: 5
      minimum-idle: 2
      connection-timeout: 30000
      idle-timeout: 600000
      max-lifetime: 1800000

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  flyway:
    enabled: true

  mail:
    host: ${MAIL_HOST:smtps.aruba.it}
    port: ${MAIL_PORT:465}
    username: ${MAIL_USERNAME:postmaster@prenota24.com}
    password: ${MAIL_PASSWORD:Gnmcl012025!}
    properties:
      mail:
        smtp:
          auth: true
          ssl:
            enable: true
        from: ${MAIL_FROM:postmaster@prenota24.com}

  task:
    execution:
      pool:
        core-size: 4
        max-size: 16

jwt:
  secret: ${JWT_SECRET:dev-only-secret-change-in-production-min-32-chars!!}
  expiration-hours: ${JWT_EXPIRATION_HOURS:24}
